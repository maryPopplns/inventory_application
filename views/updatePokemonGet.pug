extends layout
block content
  main.updatePokemonGet
    div.updatePokemonGetContainer
      header
        h3=name
      form(method='POST' action='')
        div.updatePokeNoramlField
          label(for='pokemonInstanceName') Name:
          input(id='pokemonInstanceName' type='text' name='name' value=name required='true')
        div.updatePokeNoramlField
          label(for='pokemonInstanceID') ID:
          input(id='pokemonInstanceID' type='number' name='pokeid' min=0 required='true' value=(pokeid === undefined ? '' : pokeid))
        div.updatePokeNoramlField
          label(for='pokemonInstanceHeight') Height:
          input(id='pokemonInstanceHeight' type='number' name='height' min=0 required='true' value=(height === undefined ? '' : height))
        div.updatePokeNoramlField
          label(for='pokemonInstanceWeight') Weight:
          input(id='pokemonInstanceWeight' type='number' name='weight' min=0 required='true' value=(weight === undefined ? '' : weight))
        div.updatePokeNoramlField
          label(for='pokemonInstanceHP') HP:
          input(id='pokemonInstanceHP' type='number' name='hp' min=0 required='true' value=(stats.hp === undefined ? '' : stats.hp))
        div.updatePokeNoramlField
          label(for='pokemonInstanceAttack') Attack:
          input(id='pokemonInstanceAttack' type='number' name='attack' min=0 required='true' value=(stats.attack === undefined ? '' : stats.attack))
        div.updatePokeNoramlField
          label(for='pokemonInstanceSpecialAttack') Special Attack:
          input(id='pokemonInstanceSpecialAttack' type='number' name='specialAttack' min=0 required='true' value=(stats['special-attack'] === undefined ? '' : stats['special-attack']))
        div.updatePokeNoramlField
          label(for='pokemonInstanceSpecialDefense') Special Defense:
          input(id='pokemonInstanceSpecialDefense' type='number' name='specialDefense' min=0 required='true' value=(stats['special-defense'] === undefined ? '' : stats['special-defense']))
        div.updatePokeNoramlField
          label(for='pokemonInstanceSpeed') Speed:
          input(id='pokemonInstanceSpeed' type='number' name='speed' min=0 required='true' value=(stats.speed === undefined ? '' : stats.speed))
        //-  [ MOVES / TYPES ]
        div.checkboxContainer.updatePokemonTypesContainer
          div.slider.updatePokemonTypesSlider
            p Types
            div.arrowImageWrapper
              img(src='/images/downArrow.svg')
          div.content.updatePokemonTypesContent
              each oneType in allTypes
                div
                  label(for=oneType.id) #{oneType.nameCap}
                  input(id=oneType.id type='checkbox' name='types' value=oneType.name checked=oneType.checked)
        button(type='submit') submit
      if errorsArray
        ul.errors
          for error in errorsArray
            li
              div #{error.param}:
              div=error.msg
  script(src="/javascripts/updatePokemonGet.js")